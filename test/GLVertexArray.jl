using Test
using LinearAlgebraicRepresentation
Lar = LinearAlgebraicRepresentation
using ViewerGL
GL = ViewerGL

@testset "GLVertexArray.jl" begin

   # ffunction GLVertexArray()
   @testset "GLVertexArray" begin
      @test
      @test
      @test
      @test
   end

   # function releaseGpuResources(array::GLVertexArray)
   @testset "releaseGpuResources" begin
      @test
      @test
      @test
      @test
   end

   # function enableVertexArray(array::GLVertexArray)
   @testset "enableVertexArray" begin
      @test
      @test
      @test
      @test
   end

   # function disableVertexArray(array::GLVertexArray)
   @testset "disableVertexArray" begin
      @test
      @test
      @test
      @test
   end

end
